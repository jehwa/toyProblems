/**
 * time complexity: O(nlogn);
 * worst cawse: 0(n^2) when pivot is bad. 
 */

let quickSort = (arr) => {
  let right;
  let left;

  let recursion = (array) => {
    
  }

}

let swap = (i, j, array) => {
  let temp = array[i];
  array[i] = array[j];
  array[j] = temp;
  return array;
}